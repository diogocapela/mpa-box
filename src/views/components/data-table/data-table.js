/* datatables jQuery plugin
============================================================================================= */

/* initialize datatables */
$(document).ready(() => {
    $('#dataTable').DataTable();
});
